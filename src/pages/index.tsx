import ProgressBar from '@/components/ProgressBar'

const Home = () => {
  return (
    <section
      style={{
        padding: '10px',
      }}
    >
      <ProgressBar progress={20} size="large" />
    </section>
  )
}

export default Home
